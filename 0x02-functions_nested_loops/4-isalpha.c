#include "main.h"
/**
* print_alphabet_x10 --> prints the lower case 10 times
* @c: is a character
* Return: returns 1 and 0 depending on condition
*/
int _islower(int c)
{
return (c >= 'A'&& c <= 'Z');
}
