#include "main.h"
/**
 * _isdigit- a function that checks for a digit(0-9)
 * @c : digit
 * Description: prints 1 if +ve and 0 if -ve
 * Return: void
 */
int _isdigit(int c)
{
if (c >= 9)
return (1);
else
return (0);
}
